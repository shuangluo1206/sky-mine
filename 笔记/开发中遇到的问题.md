09-08:
对于接口文档的测试，有时候不一定很准确，比如重复的数据测试，最好在前端进行测试，后端看错误信息

接口文档的数据插入，对数据库没有作用,

在接口文档里面idnumber忘了加引号，耽误大量时间

APIpost的分页查询无法生成相应体，开始怀疑是名字在数据库中不存在
后面发现只是单纯地post方法无法生成

09-09:
对于图片的上传成功了，但是前端页面看不见，最终发现是oss的允许公众访问没有开放

09-10:
解决了昨天一直烦恼的问题，上传GitHub不允许有alioss密匙，
于是写到env.lcoal里面，结构环境变量存在问题，多写了一个空格导致失败

关于接口文档分为管理端和用户端的问题，原来要在配置文件里面先写另外一个docket，
而且要写groupname，把名字给写好了，才会正确的显示

微信小程序，我的250邮箱，密码：qwer1234@

09-11:
.env.local 是 Node.js 生态（dotenv 库）的惯例。你的项目是 Spring
Boot + Maven，我查了 pom.xml——没有引入任何 dotenv 依赖，所以 Spring
Boot 根本不认识这个文件，它现在就是个躺在硬盘上的普通文本文件。

真正在起作用的只有你环境变量里那份（${ALIOSS_ACCESS_KEY_ID}、${ALIOS
S_ACCESS_KEY_SECRET} 是从真实的进程环境变量里取的）。



9.20:
1.天呐，耽误了好多的时间啊，整整十天没有新的错误学习，这里微信登录之所以没有获取头像的函数是因为函数太低级了，过期了
2.