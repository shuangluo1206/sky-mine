# 苍穹外卖 · 从零自己敲（方案 A）

工作区：`/Users/luoshuangshuang/Downloads/sky-mine`（已 git init）
参考答案：`/Users/luoshuangshuang/Downloads/sky-take-out/sky-take-out`（**只读，卡住了才翻**）
讲义：`/Users/luoshuangshuang/Downloads/sky-take-out/讲义/dayXX`
资料：`/Users/luoshuangshuang/Downloads/sky-take-out/资料/dayXX`

## 铁律

1. **先自己写，卡 20 分钟以上再翻参考答案。** 翻之前先把你卡住的具体问题写下来。
2. **不许复制粘贴。** 看完参考答案要关掉，凭理解自己敲。
3. 每天收工前，**合上所有资料，口述当天做了哪些接口、动了哪张表、为什么这么设计**。说不出来就是没懂。
4. 每完成一个功能点就 `git commit`，commit message 写清楚做了什么。这是你的学习记录，也是后面简历的素材来源。

## 版本决定

阶段一先用**和讲义一致的版本**：Spring Boot 2.7.3 + JDK 17。
理由：讲义里的代码都是 `javax.*` 时代的，你要是现在就上 Spring Boot 3，
每写一行都要自己做一次 `javax` → `jakarta` 的翻译，学习摩擦太大。
**升级到 Spring Boot 3 + JDK 21 是阶段二的任务**，那时候当成一次真实的遗留系统迁移来做，
反而是简历素材。

## 每次启动前必做

```bash
export JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home
```

不设这个，Maven 会用 JDK 26，Spring Boot 2.7.3 + lombok 1.18.30 必崩。
建议直接写进 `~/.zshrc` 省事。

---

# day01：从零搭出这个工程骨架

> 这一天不写业务逻辑，全是「工程搭建」。**别觉得没价值——面试问「你们项目怎么分模块」
> 「为什么这么分」的时候，答不上来的人一大半。**

## 目标结构

```
sky-mine/
├── pom.xml                 ← 父工程，只做依赖版本管理，packaging=pom
├── sky-common/             ← 公共模块：工具类、常量、自定义异常、统一返回结果
├── sky-pojo/               ← 实体类模块：Entity / DTO / VO
└── sky-server/             ← 业务模块：Controller / Service / Mapper / 配置，有启动类
```

## 任务清单

### 1. 父工程 pom.xml
- [ ] `packaging` 设成 `pom`
- [ ] 继承 `spring-boot-starter-parent` 2.7.3
- [ ] `<modules>` 声明三个子模块
- [ ] 用 `<properties>` 定义各依赖版本号，`<dependencyManagement>` 统一管理
- [ ] 依赖清单（照抄版本号可以，但要知道每个是干什么的）：
      mybatis-spring-boot-starter 2.2.0、lombok 1.18.30、pagehelper 1.3.0、
      druid-spring-boot-starter、knife4j 3.0.2、jjwt 0.9.1、aliyun-sdk-oss 3.10.2、
      poi 3.16、fastjson

**自测问题**（答不出就去查，别跳过）：
- `<dependencies>` 和 `<dependencyManagement>` 有什么区别？
- 为什么父工程 packaging 要是 `pom` 而不是 `jar`？

### 2. sky-common / sky-pojo
- [ ] 两个模块的 pom（`<parent>` 指向父工程即可，很短）
- [ ] sky-pojo 依赖 lombok
- [ ] sky-common 先建包结构就行：`com.sky.constant` / `utils` / `exception` / `result`
- [ ] 写一个 `Result<T>` 统一返回结果类（`code` / `msg` / `data` + 静态 success/error 方法）

**自测问题**：为什么要单独拆一个 pojo 模块，不直接放 server 里？

### 3. sky-server
- [ ] pom 依赖 sky-common、sky-pojo、web、mysql-connector、mybatis、redis、druid 等
- [ ] 启动类 `com.sky.SkyApplication`
- [ ] `application.yml`：端口 8080、`profiles.active=dev`、Druid 数据源、MyBatis 驼峰映射、日志级别
- [ ] `application-dev.yml`：数据库和 Redis 连接信息（**照下面的实测参数填**）
- [ ] knife4j 接口文档配置类

### 4. 数据库
数据库**已经导好了，不用重复导**（你 2026-06-01 导过）：

| 项 | 值 |
|---|---|
| 地址 | `localhost:3306` |
| 库名 | `sky_take_out` |
| 用户 / 密码 | `root` / **空** |
| Redis | `localhost:6379`，用 db 10 |

想重来的话源脚本在 `资料/day01/数据库/sky.sql`。

## 验收标准（做完喊我，我来跑这三条）

- [ ] `mvn install -DskipTests` 通过
- [ ] `cd sky-server && mvn spring-boot:run` 能起来，日志出现 `Started SkyApplication`
- [ ] http://localhost:8080/doc.html 能打开接口文档

## 卡住了怎么办

先自己查，实在不行按这个顺序：
1. 看讲义 `讲义/day01/`
2. 看参考答案对应文件（**看完关掉，自己重敲**）
3. 问我 —— 但要带着「我想干什么 / 我做了什么 / 报了什么错」三件事来问，
   直接扔一句「起不来」我也帮不了你

